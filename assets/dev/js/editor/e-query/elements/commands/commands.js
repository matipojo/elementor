
import Create from './create';
import Duplicate from './duplicate';
import Remove from './remove';
import Move from './move';
import Settings from './settings';

const Commands = {
	Create,
	Duplicate,
	Remove,
	Move,
	Settings,
};

export default Commands;

export { Create, Duplicate, Remove, Settings };
