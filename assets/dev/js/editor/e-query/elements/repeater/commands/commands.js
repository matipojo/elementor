import Duplicate from './duplicate';
import Insert from './insert';
import Move from './move';
import Remove from './remove';
import Settings from './settings';

const Commands = {
	Duplicate,
	Insert,
	Move,
	Remove,
	Settings,
};

export default Commands;

export { Duplicate, Insert, Remove, Move, Settings };
